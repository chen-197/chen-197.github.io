</main>
<footer class="site-footer">
  <div class="container">
    <svg class="leaf-mark" viewBox="0 0 24 24" width="19" height="19" aria-hidden="true">
      <path d="M4 20 C 4 11, 10 4, 20 4 C 20 13, 14 20, 4 20 Z" fill="currentColor"></path>
      <path class="vein" d="M5.6 18.4 C 9.6 14.4, 14 10.4, 18.4 5.6" fill="none" stroke-width="1" stroke-linecap="round"></path>
    </svg>
    <p>&copy; <%= new Date().getFullYear() %> <%= site.author %> · 由 Node.js 静态生成器驱动 · <a href="/feed.xml">RSS</a></p>
  </div>
</footer>
<% if (typeof math !== 'undefined' && math) { %>
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js"></script>
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/contrib/auto-render.min.js"
  onload="renderMathInElement(document.body, { delimiters: [{ left: '$$', right: '$$', display: true }, { left: '$', right: '$', display: false }], throwOnError: false });"></script>
<% } %>
<% if (dev) { %>
<script>
  // 本地预览热更新：轮询构建版本，变化即刷新
  let __v = null;
  setInterval(async () => {
    try {
      const r = await fetch('/__version').then((res) => res.json());
      if (__v === null) __v = r.version;
      else if (r.version !== __v) location.reload();
    } catch (e) { /* 服务重启中，忽略 */ }
  }, 1000);
</script>
<% } %>
</body>
</html>
